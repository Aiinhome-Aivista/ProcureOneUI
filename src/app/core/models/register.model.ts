export interface NavItem {
  stepNo: number;
  title: string;
  description: string;
}

export interface BusinessType {
  id: number;
  name: string;
}
export interface IndustryCategory {
  id: number;
  name: string;
}